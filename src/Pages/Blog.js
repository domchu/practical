import React from "react";
import { Box } from "@chakra-ui/react";

const Blog = () => {
  return (
    <Box style={{ fontSize: "3rem" }} className="page">
      <h2>Blog page</h2>
    </Box>
  );
};

export default Blog;
